import styles from "./page.module.css";

// Temporary placeholder home page. Replaced scene-by-scene starting with
// Phase 3 (loader) and Phase 4 (Home scenes) in docs/BUILD_PLAN.md.
export default function Home() {
  return (
    <main className={styles.page}>
      <p className={styles.line}>The Forge — under construction</p>
    </main>
  );
}
